<%= form_for(@vulnerable_software) do |f| %>
  <% if @vulnerable_software.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vulnerable_software.errors.count, "error") %> prohibited this vulnerable_software from being saved:</h2>

      <ul>
      <% @vulnerable_software.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nvd_entry_id %><br />
    <%= f.text_field :nvd_entry_id %>
  </div>
  <div class="field">
    <%= f.label :product_id %><br />
    <%= f.text_field :product_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
