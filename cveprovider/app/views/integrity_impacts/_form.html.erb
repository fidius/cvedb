<%= form_for(@integrity_impact) do |f| %>
  <% if @integrity_impact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@integrity_impact.errors.count, "error") %> prohibited this integrity_impact from being saved:</h2>

      <ul>
      <% @integrity_impact.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :impact_id %><br />
    <%= f.text_field :impact_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
